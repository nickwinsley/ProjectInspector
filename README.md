Usage Instructions:

Open the terminal on any Linux based operating system and run "./ProjectInspector.sh" in the same directory as "patterns.txt". If necessary, add any new file extensions to patterns.txt.
